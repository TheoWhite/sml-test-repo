
(*Create two empty lists than use :: to add elements look at l3 pg ~18*)

fun unzip(list_of_tuples) = if list_1 = []:
                              then nil
                            else
                              then

(*([1,3,5], [2,4,6]).*)

unzip [(1,2), (3,4), (5,6)]
